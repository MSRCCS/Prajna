This directory contains scripts that can run tests on a windows domain cluster. 

* Config.ps1 : contains the configurations that needs be properly setup before running any tests
* Test-Samples.ps1 : run the samples as tests
* image.list:  an input file used by one of the tests in Test-Samples.ps1